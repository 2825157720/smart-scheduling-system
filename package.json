{
  "name": "smart-scheduling-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env preview --local --ip 127.0.0.1 --port 3001",
    "test:frontend": "playwright test",
    "test:frontend:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "wrangler": "4.114.0"
  }
}
