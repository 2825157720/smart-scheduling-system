# 项目全局规则

- 每次改完代码后，先清掉旧进程，再用隐藏方式重启开发服务。
- 用 `start.bat` 或 `run_forever.ps1` 启动时，必须确认 3000 端口上的旧 `app.py` / `server_runtime.py` 已退出。
- 没有在真实页面、同一 URL、同一会话里确认到结果，不算生效。
- 只要用户反馈“点不开、没反应、看不见、刷新后不对”，先复现再下结论。
- 后续所有开发类改动，不管前端还是后端，都先在开发版修改和验证，确认没问题后再合并到正式版，禁止直接动正式版代码。
